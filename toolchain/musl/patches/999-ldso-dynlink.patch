--- a/ldso/dynlink.c
+++ b/ldso/dynlink.c
@@ -1020,7 +1020,7 @@
 					sys_path = "";
 				}
 			}
-			if (!sys_path) sys_path = "/lib:/usr/local/lib:/usr/lib";
+			if (!sys_path) sys_path = "/userdisk/openwrt/lib:/userdisk/openwrt/usr/lib";
 			fd = path_open(name, sys_path, buf, sizeof buf);
 		}
 		pathname = buf;

